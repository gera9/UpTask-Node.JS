doctype html
html(lang="en")
    head
       include partials/header
    body.login 
        .contenedor-formulario
            h1= pageName
            p Add your E-mail to restore your password

            if locals.messages 
                .alertas
                    each category in Object.keys(locals.messages)
                        each error in messages[category]
                            .alerta(class=`${category}`)
                                p= error

            form.caja-login(method="POST" action="/restore-pass")
                .campo
                    label(for="email") E-mail:
                    input(type="email", name="email" id="email" placeholder="someone@something.com" value= email required)
                .campo.enviar
                    input.boton(type="submit" value="Restore Password")
                .campo.acciones
                    a(href="/log-in") I already remembered it (Log In)